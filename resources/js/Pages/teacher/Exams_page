import React from 'react';
import { Head } from '@inertiajs/react';
import Layout from './Layout';

export default function EnseignantExams({ stats, exams }) {
  return (
    <Layout>
      <Head title="Enseignant Exams" />

      <div className="space-y-6">
        {/* En-tête */}
        <div className="flex flex-col md:flex-row items-start md:items-center justify-between gap-4">
          <div className="flex-1">
            <h1 className="text-2xl font-extrabold">Welcome back, Dr Bernal!</h1>
            <p className="text-sm text-gray-500">Here’s your exam schedule and updates for today.</p>
          </div>
        </div>

        {/* Statistiques */}
        <section className="grid grid-cols-2 md:grid-cols-4 gap-6">
          <StatCard title="Classes" value={stats.classes} icon="🏫" />
          <StatCard title="Exams this week" value={stats.exams} icon="📝" />
          <StatCard title="Assigned Surveillance" value={stats.surveillance} icon="👀" />
          <StatCard title="Avg Attendance" value={`${stats.attendance}%`} icon="📈" />
        </section>

        {/* Tableau des examens */}
        <section>
          <h2 className="text-lg font-semibold mb-3">My Subject's Exam Schedule:</h2>
          <div className="overflow-x-auto bg-white rounded-2xl shadow-sm border">
            <table className="min-w-full text-sm text-left">
              <thead className="bg-gray-100 text-gray-600">
                <tr>
                  <th className="px-4 py-3">Subject</th>
                  <th className="px-4 py-3">Group</th>
                  <th className="px-4 py-3">Date</th>
                  <th className="px-4 py-3">Hour</th>
                  <th className="px-4 py-3">Room</th>
                  <th className="px-4 py-3">Type</th>
                  <th className="px-4 py-3">State</th>
                </tr>
              </thead>
              <tbody>
                {exams.map((exam, i) => (
                  <tr key={i} className="border-t">
                    <td className="px-4 py-3">{exam.subject}</td>
                    <td className="px-4 py-3">{exam.group}</td>
                    <td className="px-4 py-3">{exam.date}</td>
                    <td className="px-4 py-3">{exam.hour}</td>
                    <td className="px-4 py-3">{exam.room}</td>
                    <td className="px-4 py-3">{exam.type}</td>
                    <td className="px-4 py-3">
                      <span className="text-blue-600 font-medium">{exam.state}</span>
                    </td>
                  </tr>
                ))}
              </tbody>
            </table>
          </div>
        </section>
      </div>
    </Layout>
  );
}

// Composant Statistique
function StatCard({ title, value, icon }) {
  return (
    <div className="bg-white rounded-2xl p-5 shadow hover:shadow-md transition">
      <div className="flex items-center justify-between">
        <div className="text-2xl font-bold">{value}</div>
        <div className="w-10 h-10 bg-blue-50 rounded-full flex items-center justify-center text-xl">
          {icon}
        </div>
      </div>
      <div className="mt-2 text-sm text-gray-600">{title}</div>
    </div>
  );
}
